1.Adding: Produce a secure SHA 256 hash from user provided password
2.Adding: Convert the password into hex
3.Adding: Changing the hex to produce integer tuples of size 4
4.Adding: Each tuple integer is used to encode/decode the image.
5.Adding: Colors are also added with the tuple integer values and XOR cipher.
